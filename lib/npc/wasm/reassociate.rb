# typed: strict
# type: true
# frozen_string_literal: true

module NPC
  module WASM
    # Try to look through local get/set operation
    # and eliminate the local, either because the
    # local is immediately pushed and consumed or
    #
    class Resassociate
    end
  end
end
